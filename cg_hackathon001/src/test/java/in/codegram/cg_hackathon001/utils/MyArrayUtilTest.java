package in.codegram.cg_hackathon001.utils;

import static org.junit.Assert.*;

import org.junit.Before;
import org.junit.Test;
/**
 * Write test cases for below cases.
 * 1: check for empty array, in that case null should be returned.
 * 2: check for one element, in that case same array should be returned.
 * 3: check for multiple values, in that case reverse array should be returned. 
 * 
 * Write test cases for sort method
 * 1. check for empty array
 * 2. check for all same values in array
 * 3. check for range
 * @author Amit
 *
 */
public class MyArrayUtilTest {
	
	private MyArrayUtil myArrayUtil;
	@Before
	public void setup() {
		myArrayUtil = new MyArrayUtil();
		
	}

	@Test
	public void test() {
		fail("Not yet implemented");
	}

}
